# Dotfiles (managed by chezmoi)

## Windows

- `~/scoop/persist/windows-terminal/settings/settings.json`
	- Overwritten by Windows Terminal seemingly non-deterministically
	- [Custom input actions for enabling Shift+Enter etc.](https://github.com/microsoft/terminal/issues/530#issuecomment-755917602) seem to be deprecated and automatically get migrated to be splitted into the `actions` and `keybindings` fields
